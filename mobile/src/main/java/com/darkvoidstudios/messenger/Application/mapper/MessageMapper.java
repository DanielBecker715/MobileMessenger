package com.darkvoidstudios.messenger.Application.mapper;

import com.darkvoidstudios.messenger.Application.model.MessageDto;
import com.darkvoidstudios.messenger.Domain.model.Message;

public class MessageMapper {

    //TODO
    //From domain to application
    /*public static Message mapTo(MessageDto messageDto) {
        Message message = new Message();
        message.setUuid(messageDto.getUuid());
        message.setMessage(messageDto.getMessage());
        message.setMessageDate(messageDto.getMessageDate());
        return message;
    }

    //From application to domain
    public static MessageDto mapTo(Message message) {
        MessageDto messageDto = new MessageDto();
        messageDto.setUuid(message.getUuid());
        messageDto.setMessage(message.getMessage());
        messageDto.setMessageDate(message.getMessageDate());
        return messageDto;
    }*/
}
